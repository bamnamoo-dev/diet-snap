# 📸 DietSnap (오식완 AI 스탬프 카메라) - AI Agent Guideline

이 문서는 **DietSnap** 프로젝트의 시스템 아키텍처, 비즈니스 규칙, 실기기 테스트를 통해 검증된 UX/렌더링 가이드라인을 정의한 AI 에이전트 작업 지침서입니다. 향후 이 프로젝트에서 코드를 작성하거나 기능을 확장할 때 반드시 본 지침을 준수해야 합니다.

---

## 1. 프로젝트 개요

* **서비스명**: DietSnap (오식완 AI 스탬프 카메라)
* **공식 배포 주소**: `https://diet-snap.vercel.app/`
* **깃허브 저장소**: `https://github.com/bamnamoo-dev/diet-snap`
* **목적**: 2030 여성 다이어터를 위한 초경량 인스타 감성 식단 카메라 유틸리티
* **핵심 기능**:
  1. **초기 화면 뷰파인더 룩**: 앱 실행 시 "오늘 뭐 드셨나요?"와 함께 즉시 촬영 포커싱 및 1초 체험 프리셋 제공.
  2. **1.2초 초고속 AI 식단 분석**: 사진 1장 촬영 시 메뉴/칼로리/탄단지/맞춤 칩 단일 JSON 추정 (수동 입력 0초).
  3. **100dvh 풀스크린 노스크롤 에디터**: 브라우저 스크롤 0%, 캔버스 바로 아래 템플릿 6종(성수 영수증, 핑크 라벨, 폴라로이드, 빈티지 티켓, 보그 매거진, Y2K 필름) 및 퀵 데코 스티커 바 밀착 배치.
  4. **사진 실시간 확대 및 자르기 (Zoom & Pan)**: 1.0x~3.0x 줌 툴바/핀치 줌 및 상하좌우 드래그로 원하는 음식 구도 정밀 크롭.
  5. **인스타 감성 6종 퀵 데코 스티커**: 오식완 ⭕, 클린식단 🥗, 치팅데이 🍕, 단백질 30g+ 🥩, 간헐적단식 ⏳, No Sugar 🚫 원터치 부착.
  6. **끼니 분류(아침·점심·저녁·간식·치팅) 자유로운 수정**: 에디터 바 원터치 칩 및 상세 모달에서 실시간 지정/변경.
  7. **9:16 (인스타 스토리) 기본 + 1:1 (일반 피드) 원터치 전환**: 플랫폼별 최적 규격 즉시 전환.
  8. **로컬 영구 저장소 갤러리(IndexedDB)**: 과거 식단 불러오기 시 확대/자르기 구도, 스티커, 끼니, 보정치 100% 복원 및 재편집 지원.
  9. **식단 보고서 3종**: 일간 3끼 모아보기 롱 영수증, 트레이너 쌤 제출용 A4 식단표, 주간 결산 롱 영수증.

---

## 2. 비즈니스 및 보안 규칙 (절대 준수)

1. **무로그인 초경량 1초 원칙 (No-Login Utility Principle)**:
   - 2030 여성 다이어터 타깃의 초경량 감성 카메라로서, **절대 강제 회원가입이나 로그인 화면을 도입하지 않습니다.**
   - 앱 실행 즉시 뷰파인더 촬영에 집중하며, 결제 역시 복잡한 계정 가입 없이 카카오페이/토스 간편 결제 및 활성화 코드 복원 방식을 지향합니다.
2. **서버 유지비 0원 원칙**:
   - 중앙 집중형 고비용 DB를 구축하지 않고, 클라이언트 로컬 저장소(IndexedDB / LocalStorage)를 기본으로 사용합니다.
3. **API Key 클라이언트 노출 절대 금지**:
   - Capacitor 안드로이드 APK는 쉽게 디컴파일되므로 구글 Gemini API Key를 프론트엔드 코드에 절대 하드코딩하지 않습니다.
   - 개발 환경(Vite 미들웨어 `/api/analyze`) 및 프로덕션(Vercel 서버리스 `/api/analyze.ts`) 프록시를 통해 API Key를 은닉합니다 (`x-goog-api-key` 헤더 통신 준수).
4. **과금 모델 및 한도 정책**:
   - **무료 플랜**: 하루 3장(아침/점심/저녁) 무료 촬영, 우측 하단 `DietSnap AI` 감성 워터마크 인쇄(인스타 바이럴 엔진), 기본 일간 모아보기 제공.
   - **DietSnap Pro (유료)**: **연간 5,500원(월 458원 꼴, BEST) 또는 월 990원 정기구독**, 매일 자정 15장 충전, 워터마크 100% 제거, 4종 템플릿(성수 영수증, 핑크 라벨, 폴라로이드, 빈티지 티켓) 및 유머 모드 전체 해제, 주간 오식완 롱 영수증 & 트레이너 쌤 제출용 식단표 무제한 이용.

---

## 3. 기술 스택 및 아키텍처

* **Frontend (App)**: `Vite` + `React 18` + `TypeScript` + `Tailwind CSS (CDN)` + `Lucide React`
* **Graphic Engine**: `HTML5 Canvas API`
  - 레티나 디스플레이 대응: 1080x1920 (9:16) 및 1080x1080 (1:1) 고해상도 실시간 렌더링.
* **AI Engine**: Google Gemini API (`gemini-3.5-flash-lite` 단독 고정)
  - `response_mime_type: "application/json"` 강제.
  - 응답 레이턴시: 약 1.2~1.5초.
* **Storage Engine**: `IndexedDB` (대용량 고화질 Base64 식단 영구 보관) + `LocalStorage` (세션 즉시 복원 방어).
* **Mobile Packaging & PWA**:
  - PWA Standalone 지원 (`manifest.json`, mobile-web-app-capable).
  - 향후 Capacitor 패키징 (`@capacitor/camera`, `@capacitor/share`, `@capacitor/filesystem`).

---

## 4. 실기기 검증 기반 필수 UX / 렌더링 규칙

### ① 듀얼 이미지 파이프라인 (화질과 속도 동시 달성)
* **내 폰 화면 렌더링 및 갤러리 저장**: 카메라로 촬영한 **초고화질 원본(`URL.createObjectURL(file)`)**을 100% 그대로 사용하여 음식 사진의 선명도를 완벽 보존합니다.
* **구글 API 전송**: 양념 질감(붉은 고춧가루, 김치 조각, 볶음밥 등)의 정밀 식별을 위해 백그라운드에서 **1536px, JPEG 0.88 고화질 압축본**을 전송합니다 (추가 토큰 비용 0원 유지).

### ② 100dvh 풀스크린 노스크롤 에디터 레이아웃
* **스크롤 0% 원칙**: 에디터 뷰 전체를 `h-[100dvh] w-full max-w-md overflow-hidden touch-none select-none`으로 고정하여 모바일 환경에서 가로 흔들림과 상하 스크롤을 원천 차단합니다.
* **원스톱 인터랙션**: 캔버스 바로 아래에 4종 템플릿 바, 2단 퀵 보정 바(끼니/유머/양 조절), 최하단 메인 빅 액션 바([다시 촬영] + [인스타 스토리 공유] + [JPG 저장])를 밀착 배치하여 스크롤 없이 손가락 하나로 모든 조작을 끝냅니다.

### ③ 사진 실시간 확대 및 자르기 (Zoom & Pan) 규칙
* **확대(Zoom)**:
  - 캔버스 우상단에 `[-] 1.0x~3.0x [+]` 미니 툴바 제공 및 두 손가락 핀치 줌 / 마우스 휠 줌 지원.
  - 확대 또는 이동 시 원클릭 리셋 버튼(`↺`) 자동 노출.
* **자르기 및 이동(Pan & Crop)**:
  - 상하좌우 전방향 드래그를 지원하여 원하는 음식 부위를 돋보이게 배치.
  - **폴라로이드 템플릿**: `ctx.clip()`을 적용하여 아무리 확대하거나 이동해도 흰색 액자 밖으로 사진이 나가지 않도록 완벽 크롭.
  - **영수증/티켓 템플릿**: 하단 텍스트 종이와 겹치지 않도록 상단 Safe Stage 영역을 기준으로 안착.
* **영구 동기화**: 조절된 `zoom`, `offsetX`, `offsetY`는 `SavedDietRecord`에 실시간 자동 저장되어 갤러리에서 다시 불러왔을 때도 100% 복원 및 재편집 지원.

### ④ 끼니 분류 (Meal Type) 지정 및 스마트 자동 감지 규칙
* **5종 끼니 표준**: `breakfast`(아침), `lunch`(점심), `dinner`(저녁), `snack`(간식), `cheating`(치팅).
* **스마트 시간대 자동 감지 (수동 선택 0초)**:
  - `05:00 ~ 09:59`: **아침 (`breakfast`) 🌅**
  - `10:00 ~ 11:29`: **오전 간식 (`snack`) 🍪**
  - `11:30 ~ 14:29`: **점심 (`lunch`) ☀️**
  - `14:30 ~ 17:29`: **오후 간식/티타임 (`snack`) ☕**
  - `17:30 ~ 21:29`: **저녁 (`dinner`) 🌙**
  - `21:30 ~ 04:59`: **야식/치팅 (`cheating`) 🍕**
  - 사진 분석 완료 시 현재 시간을 감지하여 위 기본값이 100% 자동 세팅되므로 별도 터치가 불필요합니다.
* **KST 로컬 날짜 엄수**: 날짜 키(`dateKey`) 생성 시 UTC가 아닌 브라우저 로컬 타임존(KST)을 강제하여 오전 9시 이전 아침 식단이 분리되지 않도록 보장합니다.
* **수정 경로**: 에디터 메인 바 2단 원터치 칩 및 `[수치/끼니수정]` 상세 모달 내에서 언제든 변경 가능.
* **보고서 연동**: 수정된 끼니는 일간 3끼 모아보기 영수증 및 트레이너 쌤 제출용 식단표에 정확하게 자동 분류되어 인쇄.

### ⑤ 9:16 (스토리) 기본 및 1:1 (피드) 전환 규칙
* **기본값**: 인스타그램 스토리 바이럴 및 세로 롱 영수증 감성을 위해 **9:16(1080×1920)**을 기본값으로 유지.
* **원터치 전환**: 캔버스 좌상단에 `[9:16 스토리 ⇄ 1:1 피드]` 토글 버튼을 상시 배치하여 피드용 정사각형(1080×1080)으로 1초 만에 전환 가능.

### ⑥ 텍스트 무손실(No Truncation) & 고시인성 보장 원칙
* **스탬프 안전 종이 영역 (Safe Paper Zone)**:
  - 사진이 어둡거나 복잡하더라도 하단 스탬프 종이 영역 전체(560px)를 **순백색(`#ffffff`)으로 완전 분리 차단**하여 사진이 글씨를 가리지 않도록 보장합니다.
* **메뉴명**: 글자 자르기(`slice`)를 금지하며, `wrapText` 헬퍼를 통해 긴 메뉴명도 **자동 줄바꿈으로 온전하게 전부 출력**합니다 (초고대비 딥블랙).
* **탄단지 매크로 카드**: 모바일에서의 압도적 시인성을 위해 **38px 초볼드 숫자 + 24px 한글 라벨 + 3px 고대비 테두리**를 적용합니다.

### ⑦ 식단 보고서 3종 아키텍처
* **위치**: 상단 헤더 우측 `[내 기록]` (식단 갤러리) 대시보드 상단에 일괄 집약.
* **① 일간 식단 보고서 (`[오늘의 N끼 모아보기 영수증 발행]`)**:
  - 오늘 촬영한 아침·점심·저녁 식단 사진(1~4분할 콜라주) + 총 칼로리/탄단지 합산 + 식사별 타임라인을 1080×1920 세로 롱 영수증으로 합성.
* **② 트레이너 쌤 제출용 식단표 (`[쌤 제출용 식단표]` - PRO)**:
  - PT 코치 카톡 전송용 A4 화이트 규격 정밀 매크로 표 (식사 사진 + 상세 영양 수치 + 피드백 박스).
* **③ 주간 식단 보고서 (`[주간 결산 영수증]` - PRO)**:
  - 최근 7일간의 일평균 칼로리, 단백질 목표 달성률, 요일별 식사 추이 바코드/차트 롱 영수증.

### ⑧ 갤러리(사진첩) 일괄 저장 및 백업 파이프라인
* **원클릭 사진첩 일괄 저장 (`[📸 영수증 사진첩에 일괄 저장]`)**:
  - 2030 여성 다이어터가 주말에 인스타 피드나 블로그에 주간 식단을 쉽게 올릴 수 있도록, 저장된 식단들을 고화질(1080x1920) 완성형 영수증 스탬프 이미지로 일괄 렌더링하여 폰 사진첩에 쏙 저장.
  - 모바일 Web Share API 및 다운로드 파이프라인을 지원하여 iOS/Android 사진첩 저장 시트와 연동.
* **영구 데이터 보존 보조 링크**:
  - 기기 변경이나 브라우저 캐시 삭제에 대비하여 JSON 파일 백업 및 원클릭 복원 파이프라인 상시 제공.
