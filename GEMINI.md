# 📸 DietSnap (오식완 AI 스탬프 카메라) - AI Agent Guideline

이 문서는 **DietSnap** 프로젝트의 시스템 아키텍처, 비즈니스 규칙, 실기기 테스트를 통해 검증된 UX/렌더링 가이드라인을 정의한 AI 에이전트 작업 지침서입니다. 향후 이 프로젝트에서 코드를 작성하거나 기능을 확장할 때 반드시 본 지침을 준수해야 합니다.

---

## 1. 프로젝트 개요

* **서비스명**: DietSnap (오식완 AI 스탬프 카메라)
* **공식 배포 주소**: `https://diet-snap.vercel.app/`
* **깃허브 저장소**: `https://github.com/bamnamoo-dev/diet-snap`
* **목적**: 2030 여성 다이어터를 위한 초경량 인스타 감성 식단 카메라 유틸리티
* **핵심 기능**:
  1. 사진 1장 촬영 시 1.2초 내 메뉴/칼로리/탄단지 자동 분석 (수동 입력 0초)
  2. 성수동 영수증 룩 & 미니멀 폴라로이드 룩 Canvas 실시간 스탬프 합성
  3. 1초 보정 칩 (`[소]`, `[보통]`, `[곱빼기]`, `[국물 제외]`) 및 상세 수치 직접 수정 모달
  4. 원본 음식 사진 및 스탬프 합성 사진 갤러리 직접 저장 & 인스타그램 스토리 즉시 공유

---

## 2. 비즈니스 및 보안 규칙 (절대 준수)

1. **서버 유지비 0원 원칙**:
   - 중앙 집중형 고비용 DB를 구축하지 않고, 클라이언트 로컬 저장소(IndexedDB / LocalStorage)를 기본으로 사용합니다.
2. **API Key 클라이언트 노출 절대 금지**:
   - Capacitor 안드로이드 APK는 쉽게 디컴파일되므로 구글 Gemini API Key를 프론트엔드 코드에 절대 하드코딩하지 않습니다.
   - 개발 환경(Vite 미들웨어 `/api/analyze`) 및 프로덕션(Vercel 서버리스 `/api/analyze.ts`) 프록시를 통해 API Key를 은닉합니다.
3. **과금 모델 및 한도**:
   - 유료 모델: **1년 3,300원 (월 500장 캡)** 정기구독.
   - 무료 버전은 은은한 `DietSnap AI` 워터마크를 우측 하단에 기본 렌더링하여 인스타 바이럴을 유도합니다.

---

## 3. 기술 스택 및 아키텍처

* **Frontend (App)**: `Vite` + `React 18` + `TypeScript` + `Tailwind CSS (CDN)` + `Lucide React`
* **Graphic Engine**: `HTML5 Canvas API`
  - 레티나 디스플레이 대응: 1080x1920 (9:16) 및 1080x1080 (1:1) 고해상도 렌더링.
* **AI Engine**: Google Gemini API (`gemini-3.5-flash-lite` 단독 고정)
  - `response_mime_type: "application/json"` 강제.
  - 응답 레이턴시: 약 1.2~1.5초.
* **Mobile Packaging & PWA**:
  - PWA Standalone 지원 (`manifest.json`, mobile-web-app-capable).
  - 향후 Capacitor 패키징 (`@capacitor/camera`, `@capacitor/share`, `@capacitor/filesystem`).

---

## 4. 실기기 검증 기반 필수 UX / 렌더링 규칙

### ① 듀얼 이미지 파이프라인 (화질과 속도 동시 달성)
* **내 폰 화면 렌더링 및 갤러리 저장**: 카메라로 촬영한 **초고화질 원본(`URL.createObjectURL(file)`)**을 100% 그대로 사용하여 음식 사진의 선명도를 완벽 보존합니다.
* **구글 API 전송**: 양념 질감(붉은 고춧가루, 김치 조각, 볶음밥 등)의 정밀 식별을 위해 백그라운드에서 **1536px, JPEG 0.88 고화질 압축본**을 전송합니다 (추가 토큰 비용 0원 유지).

### ② 사진 잘림 없는 맞춤형 프레이밍 규칙
* **식판 등 가로 사진 (`가로 > 세로`)**:
  - 가로폭(100%)을 프레임에 꽉 맞추어 식판의 맨 왼쪽 밥부터 맨 오른쪽 반찬까지 **좌우 잘림을 원천 차단**합니다.
  - 상하단 빈 공간은 순백색(`#ffffff`) 배경으로 채워 고급스러운 액자 느낌을 연출합니다.
* **바나나 등 세로 사진 (`세로 >= 가로`)**:
  - 음식 전체가 100% 온전히 보이도록 `contain` 맞춤 렌더링합니다.

### ③ 텍스트 무손실(No Truncation) & 고시인성 보장 원칙
* **스탬프 안전 종이 영역 (Safe Paper Zone)**:
  - 사진이 세로로 길거나 어두운 배경(노트북 키보드, 식탁 등)을 포함하더라도, 하단 스탬프 종이 영역 전체(560px)를 **순백색(`#ffffff`)으로 완전 분리 차단**하여 사진이 텍스트 영역을 침범하거나 글씨를 가리지 않도록 100% 보장합니다.
* **메뉴명**: 글자 자르기(`slice`)를 금지하며, `wrapText` 헬퍼를 통해 아무리 긴 메뉴명(예: 4~5가지 반찬의 급식 식판)도 **자동 줄바꿈으로 온전하게 전부 출력**합니다 (색상: `#09090b` 초고대비 딥블랙).
* **한 줄 코멘트**: 말줄임표 없이 전용 말풍선 박스 내에서 2줄 자동 줄바꿈으로 끝까지 표시합니다.
* **탄단지 매크로 카드**: 모바일에서의 압도적 시인성을 위해 **38px 초볼드 숫자 + 24px 한글 라벨 + 3px 고대비 테두리**를 적용합니다.

### ④ 저장 UX & 데이터 복원 안전장치 (자동 다운로드 팝업 차단)
* **자동 다운로드 팝업 차단**: 카메라 촬영 시 브라우저 강제 다운로드 창이 연속으로 뜨는 사용자 피로도를 방지하기 위해 자동 `click()` 다운로드를 비활성화하고, 인스타 공유 및 하단 `[고해상도 JPG 파일로 직접 저장하기]` 버튼을 통한 깔끔한 수동 저장을 원칙으로 합니다.
* **새로고침 방어**: 모바일 `overscroll-behavior-y: contain`으로 당겨서 새로고침을 차단하고, `localStorage`에 세션을 실시간 보관하여 새로고침 시에도 작업 내용이 증발하지 않습니다.
* **오차 탈출구**: 1초 칩 보정과 함께 언제든 키패드로 칼로리/중량/탄단지 숫자를 직접 바꿀 수 있는 `EditNutritionModal`을 상시 제공합니다.

### ⑤ 음식 연동형 1초 보정 칩 & 단일 API 호출 원칙 (1회 캡슐화)
* **단일 API 호출 절대 원칙**:
  - 사진 1장 분석 시 Gemini API는 오직 **단 1회**만 호출합니다.
  - 메뉴 식별, 칼로리/탄단지 추정과 함께 **음식 종류에 최적화된 맞춤형 1초 보정 칩(`custom_chips`: label, scale)**을 단일 JSON 응답에 캡슐화하여 받아옵니다.
  - 추가 API 호출 비용 0원 유지 및 1.2초 내 즉시 화면 렌더링을 보장합니다.
* **음식 종류별 연동 프리셋 예시**:
  - **식판 (급식/구내식당)**: `[초등 급식 (0.8x)]`, `[중고등 기본 (1.0x)]`, `[성인 구내식당 (1.2x)]`, `[국물 제외 (0.85x)]`
  - **커피/음료**: `[기본 (1.0x)]`, `[시럽 뺌 (0.7x)]`, `[대용량 벤티 (1.4x)]`
  - **샐러드/포케**: `[기본 (1.0x)]`, `[드레싱 뺌 (0.75x)]`, `[밥 반공기 (0.8x)]`, `[토핑 추가 (1.3x)]`
  - **과일/단품**: `[1개 기본]`, `[반 개 (0.5x)]`, `[송이 전체]`
* **스탬프 연동 인쇄**:
  - 선택된 보정 칩의 라벨(`[초등 급식]`, `[시럽 뺌]`, `[국물제외]` 등)은 캔버스 스탬프 하단(바코드 밑 및 폴라로이드 날짜 옆)에 온전하게 인쇄되어 보는 사람에게 정확한 식사 기준을 전달합니다.
