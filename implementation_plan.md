# [구현 계획서] DietSnap Pro 과금 엔진 (연 5,500원 / 월 990원) 및 3대 킬러 기능 구현

대표님께서 확정해주신 **[1년 5,500원(BEST) & 월 990원 듀얼 구독 BM]**과 **[2030 여성 타겟 3대 킬러 기능]**을 앱에 완벽히 구현합니다.

---

## 1. 주요 변경 사항 및 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│ 1. 과금 및 사용량 제어 엔진 (src/utils/subscription.ts)      │
│  - 무료 유저: 매일 자정 3장 충전 (기본 3끼)                 │
│  - Pro 유저: 매일 자정 15장 충전                            │
│  - 테스트용 원클릭 Pro/무료 스위처 제공 (실기기 검증용)      │
├─────────────────────────────────────────────────────────────┤
│ 2. 감성 Pro 구독 업그레이드 모달 (src/components/ProModal.tsx)│
│  - [BEST] 1년 정기권: 5,500원 (월 458원 꼴, 54% 할인)       │
│  - [월간] 1개월 정기구독: 월 990원                          │
│  - 혜택 명시 (워터마크 제거, 일 15장, 쌤 보고서, 주간 결산)   │
├─────────────────────────────────────────────────────────────┤
│ 3. 3대 킬러 기능 실체화                                     │
│  ① 16:8 간헐적 단식 공복 시간 자동 계산 스탬프 (수동입력 0초)│
│  ② PT / 필라테스 쌤 제출용 원클릭 리포트 (카톡 규격 표 렌더러)│
│  ③ 주간 오식완 결산 롱 영수증 (Weekly Diet Wrap 캔버스)    │
│  ④ 무료 유저 워터마크 자동 합성 및 Pro 유저 100% 제거      │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. 세부 파일별 변경 계획

### [NEW] `src/utils/subscription.ts`
- 오늘 날짜(YYYY-MM-DD) 기반 일일 촬영 사용량 추적 (`getDailyUsage`, `incrementDailyUsage`).
- 유저 상태 조회 및 변경 (`getUserPlan(): 'free' | 'pro'`, `setUserPlan`).
- 남은 장수 계산 (`getRemainingCount()`: 무료 3장 / Pro 15장).
- 공복 시간 자동 계산 헬퍼 (`calculateFastingTime(previousTimestamp?: string)`).

### [NEW] `src/components/ProModal.tsx`
- 2030 여성 타겟 성수동 감성 다크 룩 결제 모달.
- 연 5,500원(월 458원 꼴, 54% 파격할인 BEST 뱃지) & 월 990원 듀얼 라디오 선택.
- 기능별 잠금 해제 안내 및 즉각 테스트용 "Pro 모드로 전환하기" 버튼 지원.

### [MODIFY] `src/types/diet.ts`
- `DietAnalysis` 타입에 `fastingHours?: string` (예: "16h 20m") 추가.
- `UserPlan`: `'free' | 'pro'` 타입 정의.

### [MODIFY] `src/components/StampCanvas.tsx`
- 무료 플랜 유저의 경우 우측 하단에 `DietSnap AI` 감성 워터마크 자동 합성.
- Pro 플랜 유저의 경우 워터마크 100% 완전 제거.
- `fastingHours` 존재 시 스탬프 상단에 `[FASTING 16h 30m 🔥]` 감성 뱃지 자동 렌더링.

### [MODIFY] `src/components/GalleryView.tsx`
- **주간 오식완 롱 영수증 (Weekly Wrap)** 모달 & 캔버스 렌더러 추가.
  - 이번 주 총 끼니, 총 칼로리, 단백질 달성률 등 일괄 집계 영수증 합성.
  - 무료 유저에게는 Pro 뱃지 표시 및 클릭 시 ProModal 호출.
- **PT / 필라테스 쌤 제출용 원클릭 리포트 (Trainer Report)** 모달 & 캔버스 렌더러 추가.
  - 카톡 전송에 최적화된 일일/주간 식단표(아침/점심/저녁 사진 + 탄단지 표) 이미지 생성 및 다운로드.
  - 무료 유저에게는 Pro 뱃지 표시 및 클릭 시 ProModal 호출.

### [MODIFY] `src/App.tsx` & `src/components/IntroView.tsx`
- 상단 헤더에 현재 플랜 및 잔여 횟수 뱃지 (`무료 3/3장` or `👑 Pro 15/15장`) 배치.
- 3장 초과 시 카메라 촬영 및 분석 시도를 부드럽게 가로채고 `ProModal` 팝업 표시.
- 사진 촬영 후 IndexedDB의 직전 식단 타임스탬프와 현재 시간을 비교하여 공복 시간 자동 계산 및 스탬프에 주입.

---

## 3. 검증 계획

### 1. 빌드 및 런타임 무결성 검증
- `npm run build`를 실행하여 TypeScript 컴파일 및 린트 오류 0개 검증.
### 2. 브라우저 및 실기기 인터랙션 검증
- 인트로 상단 뱃지(무료 3장 잔여) 및 Pro 전환 모달 열림 검증.
- 1년 5,500원 / 월 990원 요금제 UI 및 혜택 안내 정상 노출 확인.
- 갤러리 화면에서 "PT 쌤 제출용 보고서" 및 "주간 결산 롱 영수증" 클릭 시 모달 및 캔버스 렌더링 검증.
- 직전 식사 기록 시 공복 시간 스탬프 정상 인쇄 여부 확인.
